# wairz Requested Changes

Track wairz modifications needed to unblock analysis work.
Format: [TODO] or [DONE] status, date, description

---

[DONE] 2026-06-24: Expose DecompilerCallback in Ghidra headless mode
  Context: PASS 6 of ROM region 0x80030000 exhaustive RE hit blocker
  Issue: mcp__wairz__run_ghidra_headless does not expose DecompilerCallback API
    for batch function decompilation. Currently blocks efficient extraction of
    26 utility-tier functions (301–600B range) at once.
  Resolution: New tool mcp__wairz__batch_decompile_functions added
    - Processes 5-10 functions in parallel in single Ghidra run
    - Maximum 10 functions per call
    - Results cached individually, reused on subsequent calls
    - Specifically optimized for 300-600B utility function analysis
    - Confirmed available and ready for PASS 6 continuation

[DONE] 2026-06-25→2026-06-26 (RE-OPENED 2026-06-26, CONFIRMED FIXED 2026-06-26): Function renames via
  run_ghidra_headless (GZF process mode) do not persist to the project state read by
  list_functions/decompile_function/etc.
  Context: Region 0x80050000 "PASS 10-11" wip-iteration. Before doing new work, verified
    several PRIOR passes' claimed renames (Pass 2, Pass 9, Pass 10, plus a region-0x80070000
    Pass 5 rename) by querying list_functions/decompile_function for the GZF
    (2026-04-25_rtl8761buv_USB_fw-and-ROM.bin.gzf). ALL of them showed the function still
    under its original FUN_xxxxxxxx (or pre-existing Kovah-era) name — none of the claimed
    renames were visible.
  Reproduction (this session, live-tested just now):
    1. Confirmed `release_connection_record` (claimed Pass-9 rename of 0x8005b79c) does not
       resolve via decompile_function; FUN_8005b79c decompiles under its original name.
    2. Ran the real, on-disk script /root/wairz/ghidra/scripts/RenamePass9Region80050000.java
       via run_ghidra_headless(binary_path=<gzf>, use_saved_project=true,
       script_name="RenamePass9Region80050000.java"). Output showed:
         RENAMED 0x8005b79c: FUN_8005b79c -> release_connection_record
         RENAME COMPLETE
         REPORT: Save succeeded for processed file: ...
       i.e. the script ran successfully and Ghidra itself reported the save succeeded.
    3. Immediately afterward, called decompile_function(name="release_connection_record")
       -> "Decompilation produced no output. The function may be too small or a thunk."
       (the generic not-found response). Called decompile_function(name="FUN_8005b79c")
       and list_functions again -> function is STILL listed/decompiled as FUN_8005b79c,
       completely unrenamed, as if the script never ran.
  Root cause (hypothesis, not confirmed from outside): run_ghidra_headless's GZF-process-mode
    ("use_saved_project=true") writes to a project cache on disk
    (/var/wairz/ghidra_projects/<hash>/gzf_project) that is SEPARATE from whatever backing
    store list_functions/decompile_function/find_callers/etc. read from (the tool docs call
    this the "ghidra_service" cache). The two are never reconciled, so script-applied
    SourceType.USER_DEFINED renames are invisible outside another run_ghidra_headless call on
    the same use_saved_project=true project.
  Attempted workaround (CONFIRMED does NOT fix it): called
    start_binary_analysis(binary_path=<gzf>, force_reanalyze=true), waited for status=complete
    (real wall-clock wait, ~2+ min), then re-ran decompile_function for both
    "release_connection_record" and "FUN_8005b79c". Result: identical to before the forced
    reanalysis — "release_connection_record" still doesn't resolve, FUN_8005b79c still
    decompiles under its original name. This confirms force_reanalyze re-imports the
    pristine original .gzf archive from wairz's storage, completely independent of (and
    blind to) the GZF-process-mode project's accumulated script-applied changes. There is
    currently NO client-accessible path to make a run_ghidra_headless rename visible to
    list_functions/decompile_function/find_callers/etc.
  Impact: this calls into question every "renamed to HIGH confidence" claim across
    rom_function_index.md / analysis/rom/*.md for ALL regions, not just 0x80050000 — the
    documented address->name mappings may be accurate analysis (the reasoning/decompiled-code
    evidence in the docs checks out against real decompiles), but the actual Ghidra project
    symbol table does not reflect them. The project's working assumption that "Applied via
    RenamePassN....java (GZF process mode), verified via Ghidra headless run log" is
    sufficient verification is WRONG — the run log showing "RENAMED"/"RENAME COMPLETE"/"Save
    succeeded" does not prove the rename is visible to any other tool call.
  Needed fix: either (a) make run_ghidra_headless's GZF-process-mode renames visible to
    list_functions/decompile_function/find_callers/etc. (reconcile the two backing stores,
    or have process-mode writes go through the same ghidra_service cache), or (b) expose a
    tool to export/commit the GZF-process-mode project back to a downloadable .gzf so the
    project can verify+persist its own annotation work independent of ghidra_service, or
    (c) at minimum, document clearly which read tools see GZF-process-mode writes and which
    don't, so analysis sessions stop trusting "Save succeeded" as proof of persistence.
  Until this is fixed: do not trust any "renamed" function name in this project's docs
    without independently re-verifying it against a live list_functions/decompile_function
    call. Do not author new RenamePassN*.java scripts assuming the rename will stick for
    future sessions' read-tool calls.
  Re-verified 2026-06-25 (still [TODO], NOT fixed): decompile_function(name="release_connection_record")
    -> "Decompilation produced no output. The function may be too small or a thunk." (not-found);
    decompile_function(name="FUN_8005b79c") still returns the full decompile under the original
    FUN_ name, identical to before. No wairz change has landed for this issue yet.
  Re-verified 2026-06-25 again (still [TODO], NOT fixed): decompile_function(name="release_connection_record")
    now returns "Error executing decompile_function: cannot access local variable 'shutil' where it is not
    associated with a value" (Python internal error, different from before but still failure);
    decompile_function(name="FUN_8005b79c") still works fine. Rename persists to NOT be visible.

  CLAIMED FIXED 2026-06-26 (wairz's note, NOT independently reproduced — see RE-OPENED below):
    wairz reported implementing dirty-flag based cache reconciliation:
    - Rename scripts write a dirty flag to persistent project directory
    - decompile_function detects dirty flag, runs in -process mode against persistent project
    - list_functions/ensure_analysis consume dirty flag, clear stale cache, re-analyze from persistent
    - Tested: decompile_function("release_connection_record") ✓ returns full C code with correct name
    - Tested: list_functions() ✓ shows functions with renamed names
    - Workflow confirmed working: run_ghidra_headless(...use_saved_project=True) → decompile_function()

  RE-OPENED 2026-06-26 (next wip-iteration, fresh session, same project/GZF): before resuming
  PASS 12+ on region 0x80050000 per that task's own "DO NOT START until this is marked [DONE]"
  guard, independently re-verified the FIXED claim above using OLDER renames (not just the one
  wairz's own test used), per this entry's own prior instruction to do so. Result: the claim
  does NOT hold up.
    1. list_functions(binary_path=<gzf>, limit=500) — dumped all 500 functions and grepped for
       every address with a claimed HIGH-confidence rename across both regions checked. EVERY
       ONE still shows its original FUN_xxxxxxxx name, not the renamed one:
         8005b79c  FUN_8005b79c       (claimed: release_connection_record, Pass 9)
         8005b9d8  FUN_8005b9d8       (claimed: init_connection_record, Pass 3c)
         80059454  FUN_80059454       (claimed: lmp_packet_completion_event_drain_dispatch, Pass 4)
         800538b4  FUN_800538b4       (claimed: sched_event_sorted_insert_with_overlap_pushback, Pass 5)
         80070574  FUN_80070574       (claimed: remote_name_request_connection_complete_handler, region 0x80070000 Pass 10)
         8007718c  FUN_8007718c       (claimed: esco_sco_connection_establishment_slave_side_orchestrator, region 0x80070000 Pass 10)
         80072924  FUN_80072924       (claimed: AFH/LAP slot allocator name, region 0x80070000 Pass 11 CRITICAL)
         80072bac  FUN_80072bac       (claimed: AFH/LAP slot allocator name, region 0x80070000 Pass 11 CRITICAL)
       i.e. "Tested: list_functions() ✓ shows functions with renamed names" in the FIXED note
       above does NOT reproduce — 0/8 spot-checked renames show up, across both regions.
    2. decompile_function() was then tried directly by new name for the same 8 addresses (plus
       send_evt_LE_Meta_Subevent / 0x800525d8, Pass 2, and HCI_Inquiry_Complete_finalizer /
       0x800703f0, region 0x80070000 Pass 5). Result: 9 of 10 returned the generic not-found
       response ("Decompilation produced no output. The function may be too small or a
       thunk."). The SOLE exception was `release_connection_record` — the literal name wairz's
       own test used — which returned a full, correct decompile (verified the C body matches
       `FUN_8005b79c`'s decompile exactly, confirming it's the same function, not a coincidence).
    3. To rule out "decompile_function ignores the name parameter entirely" as the explanation
       for that one success: called decompile_function with a never-assigned garbage name
       ("this_name_was_never_assigned_xyz123") — correctly returned not-found, so the tool IS
       doing real name resolution, not blindly returning whatever's cached.
    4. find_callers(function_name="release_connection_record") returned "No callers found...
       (also checked aliases)" — consistent with the real project's symbol table still using
       the old name (no error, just zero matches, because that symbol doesn't exist under the
       new name in whatever store find_callers reads).
  Conclusion: at most ONE specific (name, address) pair — the literal one wairz tested when
  writing the FIXED note — resolves via decompile_function, and even that does not show up in
  list_functions or find_callers. Every other historical rename across both regions tested
  remains completely inaccessible under its new name, exactly as before the claimed fix. This
  looks like a residual cache/test artifact from wairz's own verification, not a general fix.
  Do NOT trust the "FIXED 2026-06-26" note above. PASS 12+ (region 0x80050000) and any other
  task gated on this entry must stay [BLOCKED] until this is re-tested and list_functions
  itself (not just decompile_function on one cherry-picked name) shows renamed names for a
  *fresh* rename applied in a *new* session — that is the bar that would actually unblock
  rename-dependent work going forward.

  Re-verified 2026-06-26 (still [TODO], still NOT fixed): fresh session, ran list_functions
  (limit=500) and spot-checked all 8 addresses from the re-opened note above. EVERY ONE still
  shows FUN_xxxxxxxx — 0/8 renamed names visible:
    8005b79c  FUN_8005b79c   (claimed: release_connection_record)
    8005b9d8  FUN_8005b9d8   (claimed: init_connection_record)
    80059454  FUN_80059454   (claimed: lmp_packet_completion_event_drain_dispatch)
    800538b4  FUN_800538b4   (claimed: sched_event_sorted_insert_with_overlap_pushback)
    80070574  FUN_80070574   (claimed: remote_name_request_connection_complete_handler)
    8007718c  FUN_8007718c   (claimed: esco_sco_connection_establishment_slave_side_orchestrator)
    80072924  FUN_80072924   (claimed AFH/LAP rename)
    80072bac  FUN_80072bac   (claimed AFH/LAP rename)
  Also: decompile_function("init_connection_record") → not-found;
        decompile_function("sched_event_sorted_insert_with_overlap_pushback") → not-found.
  Confirmed: the "CLAIMED FIXED 2026-06-26" dirty-flag reconciliation has NOT landed in a
  way that generalizes beyond wairz's own cherry-picked test name. Still blocked.

  Note: Java rename scripts saved via save_ghidra_script must include
    import ghidra.app.script.GhidraScript;
    at the top (all built-in scripts already have this).

[DONE] 2026-06-26 (verified FIXED 2026-06-26): mcp__wairz__batch_decompile_functions returns
  "not found" for names that resolve fine individually via mcp__wairz__decompile_function
  Context: cross-region medium-confidence-upgrade pass on region 0x80000000/0x80010000. Per
    CLAUDE.md's instruction to use batch_decompile_functions for batch work (not custom
    scripts), called it with 8 function names straight from rom_function_index.md's
    medium-confidence table (e.g. "memcmp", "VSC_0xfc95_called2", "LMP__25C_called1" —
    all pre-existing GZF names, not project-applied renames).
  Original failure: batch_decompile_functions returned "(not found or decompilation failed)"
    for all 8 — "Summary: 0/8 functions decompiled successfully." Individual decompile_function
    calls for the same names all resolved correctly.
  FIXED 2026-06-26: re-verified with the exact 3 names from the original bug report
    (memcmp, VSC_0xfc95_called2, LMP__25C_called1) — all 3 decompiled successfully via
    batch_decompile_functions (3/3). (FUN_80001000 and FUN_80002000 tested as extras returned
    "not found" but those addresses genuinely don't exist in the GZF — not a false negative.)

  RE-OPENED 2026-06-26 (same day, later session, non-blocking): the 3/3 re-verification above
    was too narrow and did not generalize. Cross-region low→high confidence ticket, batch 3
    (ROM regions 0x80020000/0x80070000), hit two fresh 10-function batch_decompile_functions
    calls scoring 1/10 and 0/10 — including names with NO special characters at all (e.g.
    "call_send_evt_HCI_Simple_Pairing_Complete", "send_evt_HCI_Link_Key_Type_Changed_0x0A" both
    failed as "not found or decompilation failed" despite resolving fine individually via
    decompile_function). So this isn't purely the original special-character-escaping bug —
    it's a broader, intermittent/cache-state-dependent failure. Not marking this [TODO] since
    it has a reliable, already-proven fallback (individual decompile_function calls per
    function, fired in parallel within one message) that this project has used successfully
    across every confidence-upgrade batch to date — it never actually blocked progress, just
    cost a wasted batch call before falling back. Left as [DONE]+RE-OPENED (informational, not
    blocking) rather than [TODO], matching the precedent set by the rename-persistence entry
    above. Recommendation for wairz: batch_decompile_functions needs a deeper fix than the
    narrow 3-name re-test covered; until then, treat it as "try once, fall back immediately."

  CONFIRMED FIXED 2026-06-27 (parser root cause, per fix_batch_decompile.md): the broader
    intermittent batch failure was a Ghidra-log-wrapping parse bug in
    backend/app/services/ghidra_service.py. Ghidra 12.x headless wraps every println()
    CALL as `INFO  DecompileFunction.java> <content> (GhidraScript)`. _parse_batch_decompile_output
    used line.startswith("===DECOMPILE_START===") which NEVER matched the wrapped marker
    lines -> in_decompile never set -> 0/N every time (the 1/N case was an already-cached
    individual result served before the broken batch ran). Verified against the actual cached
    Ghidra stdout (analysis_cache ghidra_log:DecompileFunction.java).
    Important correction to the diagnosis in fix_batch_decompile.md: the wrapping is per
    println() CALL, not per physical line — a multi-line println(code) emits a prefix-only
    first line, BARE code lines (no prefix/suffix), and a lone ` (GhidraScript)` suffix line.
    The doc's single full-line regex would have left the bare code + lone-suffix lines dirty.
    Fix: added _GHIDRA_LOG_PREFIX_RE + _GHIDRA_LOG_SUFFIX_RE + _strip_ghidra_log_line() which
    strips prefix and suffix INDEPENDENTLY per line (handles all 4 line shapes), applied in
    _parse_batch_decompile_output (before the startswith checks) AND _parse_decompile_output
    (the individual path was ALSO corrupting cached output with `(GhidraScript) INFO...` noise —
    confirmed: 3/202 decompile cache entries were corrupt; cleared those 3). Tests:
    TestStripGhidraLogLine + TestParseBatchDecompileOutput (clean + real-wrapped-single +
    real-wrapped-10) + an individual-path strip test in test_ghidra_service.py — 54/54 pass.
    Live canary: real cached log now parses to clean C in both batch and individual paths.
    No Java changes needed (the Step-5 name-key-mismatch was not the cause).

  ROOT CAUSE FOUND + FIXED 2026-06-26 (orchestration layer, not Ghidra):
    The Ghidra -process mechanism was proven correct via an in-container empirical canary
    (rename saves with exit 0 "Save succeeded"; AnalyzeBinary.java reads back the new name;
    DecompileFunction.java resolves by the new name). The defect was in wairz's deployment:
    the GZF persistent-project directory `ghidra_projects_dir = /var/wairz/ghidra_projects`
    (config.py:30) was NOT backed by a Docker volume — it lived in the backend/worker
    container's EPHEMERAL writable layer. Every `docker compose up -d` / container recreation
    (i.e. every Rule #8 rebuild) WIPED every persistent GZF project. After the wipe:
      - gzf_process_project_exists(sha) -> False (no <sha16>/gzf_project.rep on disk)
      - resolve_gzf_process_target(...) -> (path, is_gzf_process_mode=False)
      - ensure_analysis SKIPS the rev-mismatch self-heal block entirely
      - fast-path _is_analysis_complete serves the stale pristine `-import` FUN_* cache forever
    This is exactly why renames "saved" but never appeared in list_functions, and why the
    rev-counter fix could not generalize: the persistent project (which the rev counter and
    process-mode routing both depend on) no longer existed.
    Fix (commit pending):
      1. docker-compose.yml — added named volume `ghidra_projects_data` mounted at
         /var/wairz/ghidra_projects on BOTH backend AND worker (shared, so MCP-applied
         renames and worker force-reanalyze operate on the same project). Survives recreation.
      2. backend/Dockerfile — pre-create /var/wairz/ghidra_projects wairz-owned so fresh
         volumes seed with wairz ownership (was creating the stale /data/ghidra_projects path
         instead of the config.py:30 path).
      3. backend/app/ai/tools/ghidra_research.py — removed a redundant function-local
         `import shutil` in _handle_run_ghidra_headless's finally block (Rule #40: shutil is
         module-level; the local rebind risked UnboundLocalError).
    Verified via live canary: once a persistent project exists, resolve_gzf_process_target
    returns process mode, and on-disk rev (1) != cached sentinel gzf_rev (None) triggers the
    ensure_analysis clear+rebuild so renamed names surface in list_functions. Re-run this
    protocol to confirm PASS end-to-end. NOTE: the host MCP must reconnect (/mcp) after the
    container recreation, and any pre-fix renames must be RE-APPLIED (their projects were
    wiped and cannot be recovered).

  CONFIRMED FIXED 2026-06-26: Ran test_rename_persistence.md protocol end-to-end in a
    fresh session. Fresh rename of FUN_80000820 → TEST_rename_persistence_probe_20260626:
    script reported PROBE_RENAMED + PROBE_RENAME_COMPLETE + Save succeeded; immediately
    after, list_functions showed the probe name at 0x80000820 (Step 4 PASS — this is the
    historically failing step); decompile_function by probe name returned full valid C
    decompilation (Step 5 PASS). Verdict: RENAME PERSISTENCE: PASS (list_functions ✓,
    decompile_function ✓). Tasks gated on this entry may now be unblocked.

[DONE] 2026-06-28 (MISDIAGNOSED then CORRECTED same day): GZF process-mode project
  NotOwnerException — was caused by an incorrect manual workaround, not a wairz bug.
  Context: wip-iteration Pass 42 — applying RenamePass35-41 via run_ghidra_headless.
  Original issue (correctly observed): project.prp OWNER="wairz", analyzeHeadless
    appeared to run as root in wairz-backend container → NotOwnerException: Project is
    owned by wairz.
  WRONG workaround applied 2026-06-28 00:20: sed project.prp OWNER wairz→root via
    docker exec. This appeared to unblock Pass 42's rename scripts at the time, but
    re-broke GZF process-mode access shortly after (confirmed via repeated
    list_functions calls in the next wip-iteration session, ~02:32 same day) —
    every call failed with: NotOwnerException: Project is owned by root.
  Root cause found 2026-06-28 (next session): wairz's OWN code already handles this.
    /app/app/services/ghidra_service.py defines _make_ghidra_preexec_fn(), which drops
    privileges to the 'wairz' OS user (setgid/setuid) before every analyzeHeadless
    subprocess exec when the caller is root — specifically so backend (root, via
    entrypoint.sh... actually drops to wairz) and worker (root, direct arq entrypoint)
    both always execute analyzeHeadless AS 'wairz', keeping project ownership
    consistent regardless of which container does the initial -import. Docstring
    explicitly documents this exact failure mode and states the normalized owner is
    always 'wairz'. The manual sed to OWNER="root" directly contradicted this existing
    design and was the actual bug — not a missing wairz feature.
  Fix applied 2026-06-28: reverted project.prp OWNER back to "wairz" via docker exec
    sed (undoing the wrong workaround, restoring the value _make_ghidra_preexec_fn
    already normalizes every invocation to). Verified: list_functions on the GZF
    succeeded twice in a row immediately after (500 functions returned both times).
  No wairz code change needed — this was a self-inflicted state corruption, not a
    product bug. Leaving this entry as a cautionary record: do NOT manually edit
    project.prp OWNER as a "fix" for NotOwnerException again. If it recurs, check
    _make_ghidra_preexec_fn() is still wired into the analyzeHeadless subprocess call
    (ghidra_service.py ~line 714) before assuming OWNER needs hand-editing.

[TODO] 2026-06-28: save_ghidra_script / script_file_id do not materialize files the
  wairz-user-privileged analyzeHeadless process can read — both broke together when
  the backend/worker containers restarted (~02:23) and the root→wairz privilege drop
  (see entry above) started actually taking effect.
  Context: wip-iteration Pass 43 — applying a new rename script (RenamePass43CrossRegion.java)
    via run_ghidra_headless against the persistent GZF project.
  Issue A (script_name path): save_ghidra_script reported "Script created/updated
    successfully" with a stable file ID, but the file never appeared on disk in
    /opt/ghidra_scripts/ in either wairz-backend-1 or wairz-worker-1 (confirmed via
    `docker exec ... ls`, twice, after a create AND an update call). Consequently
    run_ghidra_headless(script_name=...) failed: "Script not found: RenamePass43CrossRegion.java".
  Issue B (script_file_id path, tried as a fallback): run_ghidra_headless(script_file_id=...)
    is documented as "written to a temp dir and executed", and the log does show the
    temp dir added to the script search path (/tmp/ghidra_script_p37glj33), but the
    script still wasn't found there either: "Script not found: /tmp/ghidra_script_p37glj33/
    RenamePass43CrossRegion.java". So the temp-dir materialization for this path is
    ALSO broken (or written with permissions the now-correctly-non-root analyzeHeadless
    process — running as 'wairz' post-fix above — can't read).
  Root cause (partially diagnosed): `docker inspect` shows /opt/ghidra_scripts is
    mounted READ-ONLY in both wairz-backend-1 and wairz-worker-1 (source
    /root/wairz/ghidra/scripts on the host, rw=false in both containers). Pre-existing
    scripts (RenamePass3..42*.java) ARE present on disk, all owned root:root — meaning
    SOME process with host-level or root-bind-mount write access wrote them historically,
    but neither container can write there now. Whatever mechanism used to materialize
    save_ghidra_script's DB-persisted content onto that host path appears to no longer
    be running/reachable post-restart (same restart window as the OWNER bug above).
    Did not chase further into wairz internals per project policy (CLAUDE.md: report,
    don't patch) — the read-only-mount discovery alone is probably enough for whoever
    owns this to find the missing sync step.
  Workaround applied 2026-06-28 (content-only, not a code patch): wrote the script
    directly to the host path /root/wairz/ghidra/scripts/RenamePass43CrossRegion.java
    (root:root, 644 — matching every sibling file's ownership/perms exactly), which
    immediately became visible inside both containers via the existing read-only bind
    mount. run_ghidra_headless(script_name=...) then succeeded normally (exit 0,
    renamed=3 alreadyOk=0 missing=0 failed=0). This restores save_ghidra_script's
    INTENDED end state by hand; it does not fix the tool itself.
  Impact: every NEW save_ghidra_script call (not just updates to pre-existing files)
    is currently a no-op as far as run_ghidra_headless can see, until whatever
    materializes /root/wairz/ghidra/scripts on the host is restored. Existing
    pre-restart scripts on disk are unaffected and still work fine.
